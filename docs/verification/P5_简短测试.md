# P5 真实只读验收（3 步）

2026-09-06 已通过，当前操作者无需重复。以下保留供新环境参考；活动进度以 [MASTER](../progress/MASTER.md) 为准。

本次只需 Windows + PowerShell 7.2 以上 + Node.js 24；不需要 Ubuntu。只读脚本自动创建的临时文件，不读写你的项目。

1. 在 Chrome 扩展管理页**禁用旧 DeepSeek++，不要卸载**。点“加载已解压的扩展”，选择 `C:\temp\deepseek-pp-p5-5e9e641\dist\chrome-mv3`，复制新扩展 ID。在**安装此扩展的同一个 Chrome 用户配置中**新开 DeepSeek 网页，确认已登录；先在网页发送一句普通的“你好”，等正常回复，再继续。不要只在应用侧边栏浏览器中登录。
2. 在 PowerShell 执行：

   ```powershell
   cd 'C:\Users\worker\Documents\deepseek+++++'
   .\scripts\start-dsh-web-smoke.ps1 -ConfirmRealWeb -ReadOnlyTools
   ```

3. 按提示粘贴**新扩展 ID**；在新扩展“设置 → 本机 Harness”启用桥接，端口 `43123`，配对令牌按 Ctrl+V 粘贴并保存。回终端按回车；若显示离线，准备完成后再点一次保存。

把最后一行 JSON 发给我即可。预期 `ok: true`、`status: completed`、`model_steps: 2`、`tool_calls: 1`、`tool_results: 1`。失败时保留输出，不必反复重跑或删除记录。

若报 `DEEPSEEK_AUTH_REQUIRED`：这是扩展尚未取得网页登录认证，不是要求配置 API key。无需重装或删除数据；先完成第 1 步的网页普通对话，再重跑一次。仍失败就停下，反馈“网页普通对话是否成功”和最后一行 JSON，不要发送 Cookie、登录令牌或配对令牌。

这是新版扩展的真实连接验收；断线、取消、进程崩溃和重复写入保护由自动测试覆盖，不需要你手动制造失败。构建来源为 `5e9e641`；后续 `08e651e`、`4fa7ac7`、`6ccffae` 只改变测试、smoke 和验收校验，不改变浏览器生产代码。
