# T9 本机诊断版

此版本只用于定位“流不完整”和后续会话被隔离的原因，不是完整 T9 修复或正式发布。

## 用户只需协助一次扩展更新

以下 v1 更新步骤是首次诊断记录；后续 v2 构建和交接见文末，以主 agent 提供的新目录为准，不重新安装旧诊断包。

1. 告诉编排 agent：Chrome 扩展详情中的“加载来源”目录。
2. 诊断版构建在 `C:\Users\worker\Documents\deepseek-t9-diagnostic-20260909\chrome-mv3`。保留原扩展 ID：先备份原目录，再将该目录内容复制到原目录；不要卸载扩展或重新配对。
3. 在 Chrome 扩展管理页点击该扩展的“重新加载”，刷新已登录的 DeepSeek 网页；然后告诉 agent“已重新加载”。

浏览器工具不能操作扩展管理页，因此此步骤需要用户手动完成。安装 Harness 插件和后续真实只读测试由 agent 负责。当前运行的服务、配对和会话在用户准备好前保持原样。

## 诊断含义

新版错误中的 `reason=deepseek_stream_incomplete.v1:...` 只含 HTTP 状态、响应分类、字节数、SSE 事件数以及受限数字错误码。`json_error` 表示收到 JSON 业务错误；`sse_error` 表示流中的错误信号；`sse` 表示流没有受识别的完成标记。不能仅凭这些分类猜测上游错误码含义。

旧请求异常后出现 `reason=SESSION_QUARANTINED`，表示后续请求被隔离状态阻止，并不是又执行了一次模型任务。

不采集 Cookie、Authorization、PoW、完整提示词或原始响应正文。不要上传 HAR、凭据文件或包含令牌的地址。失败后不要反复点“继续”；agent 将保留证据后分析。

## 2026-09-09 本机实测记录

- 用户已原位重新加载；加载来源目录 100 个文件与 `89fce7b` 诊断构建逐一散列一致。Harness 已安装的插件 `lib/index.js` SHA-256 为 `c5fbf403c207636748b12ea04a4733f314a1d14dc33a11313b6de01fcfa7ebf7`，与该构建一致。
- 安装时 pnpm 打印 Done 后未退出，外层 60 秒硬限已清理其进程树；这不是安装命令成功退出的证据。另行核对依赖路径、bundle 列表和插件运行文件后才重启服务，保留安装前 profile 配置备份。不将安装器挂起与模型中断视为同一根因。
- 在 `http://127.0.0.1:3080/`、Default / Thinking off、仅可查看模式下，新会话 `session-39a7d0c5-6eaf-46bc-9f06-be5b80b3fe8d` 完成两轮：10 次 glob / 11 模型步；42,952 字符合成输入后 8 次 glob / 9 模型步。18 次工具调用和结果全部成功，两个 turn/end 均为 completed。
- 使用官方 JsonlSessionPersistence 解码完整多帧日志后核对；解码 JSONL SHA-256 为 `57efcb5936d87c5af684e94cdfaf3d72bfdfc985c60819d054682acb321efe63`。仅执行人工 glob 路径检查，未读取文件正文、写文件或运行命令。原始日志不纳入 Git。
- 本次未复现首次中断，未取得失败响应分类；不能宣称 T9 整体修复或判断服务端上下文超限。后续先完成已确认的隔离拒绝分类修正，故障取证仍保留。

## 用户继续测试后的首次失败

- 同一会话第三轮已复现：前 5 次 glob 均成功，第 6 个模型步骤报 `WEB_MODEL_AMBIGUOUS`，`request=8f3559785a3dc6b3`，`reason=deepseek_stream_incomplete.v1:h200:sse:b311:e3:cn:bizn`，263ms。完整持久日志解码 SHA-256 更新为 `902ef6c292dbce6188a23880ef549b5c3463a21ec11a7bcc6d95d574c37c2241`。
- 响应是 HTTP 200、311 字节、3 个 SSE 事件，没有被现有解析器识别的完成标记；code/bizCode 为 null 不代表服务端未报错，只代表该有限字段探测未取到数字码。不能归因为本地工具错误、生成超时或已经证实的上下文超限。
- 现有诊断没有保留事件原文，不能事后从 Harness 日志恢复它。请求由 `entrypoints/background.ts` 中的客户端发起；若需人工 Network 取证，检查 Chrome 扩展的 Service Worker，而非 Harness 网页。只提供相关响应事件，不能导出包含凭据的 HAR/请求头；失败旧链不重放。

## v2 事件形状取证

用户确认 Network 未留存，因此补充三项固定数值（同时存在），不记录原事件名称、路径、内容或凭据。v2 原因沿用原 h/b/e/c/biz 字段，末尾追加 `:j<JSON成功数>:k<事件类别位图>:s<结构位图>`。旧 v1 原因仍可读取，不能仅凭新位图就放行工具或宣告模型完成。

事件类别 k：message/缺省=1、ready=2、update_session=4、error=8、done=16、finish=32、close=64、其他=128。位图只表示曾出现，不表示事件顺序或成功状态。

结构 s（按位或）：

| 位值 | 观察到的形状 |
| --- | --- |
| 1 / 2 / 4 | JSON 解析失败 / 合法非对象（含数组） / 对象 |
| 8 / 16 / 32 / 64 / 128 | 根 p 缺省 / response/status / quasi_status / response / 其他 |
| 256 | 根 o=BATCH 且 v 为数组 |
| 512 / 1024 / 2048 / 4096 | 根 v 是字符串 / 数组 / 非 null 对象 / 其他 |
| 8192 | 根 v 精确等于 FINISHED |
| 16384 / 32768 | 直接 BATCH 子项 p=response/status / quasi_status 且 v 精确为 FINISHED |

不遍历任意嵌套结构；未知类别仍是未知，不能据此猜测服务端业务错误含义。解析成功数包括合法的 null、false、0，即使原生成解析流程会跳过这些值。生成完成条件、隔离及禁重试策略保持原样。

v2 已构建（源码 `bc298032d0430b69dd112904577d6979659dbea8`），Chrome 文件目录为 `C:\Users\worker\Documents\deepseek-t9-diagnostic-v2-20260909\chrome-build\chrome-mv3`。构建产物及简短更新说明均在该项目外目录中；100 个文件散列核对、59 个文本 UTF-8/JS ASCII 检查通过，manifest 与前一诊断版一致。Chrome ZIP SHA-256 `b1d62e0fb88881d1f23ff395055714b609150b89ad1271e098676f9a480ec34f`；插件 TGZ SHA-256 `36360fcc0270bfd18db687d723c6df9893749e2043ca59fe3066d02bdd3c0673`。

交接时尚未安装 v2：用户原位复制并重新加载 Chrome 后告知主 agent，再由主 agent 更新 Harness 插件并做真实诊断。新版 Host 接受旧 v1 和新 v2，但旧 Host 不识别 v2，不能只更新浏览器后直接当作完整升级验收。配对、原始失败记录和旧构建均保留；未 push/发布。
